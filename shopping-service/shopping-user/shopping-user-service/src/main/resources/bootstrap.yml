spring:
  profiles:
    active: dev
  application:
    name: shopping-user-service
  datasource:
    url: jdbc:mysql://10.1.8.200:3306/ums?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
server:
  port: 8085
dubbo:
  application:
    name: shopping-user-service
seata:
  application:
    name: shopping-user-service
logging:
  level:
    root: INFO
  file:
    name: logback-spring.xml