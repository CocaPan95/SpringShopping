spring:
  profiles:
    active: dev
  application:
    name: shopping-sms-service
  datasource:
    url: jdbc:mysql://10.1.8.200:3306/sms?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
server:
  port: 8083
dubbo:
  application:
    name: shopping-sms-service
seata:
  application:
    name: shopping-sms-service
logging:
  level:
    root: INFO
